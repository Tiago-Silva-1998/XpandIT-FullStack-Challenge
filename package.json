{
  "name": "xpandit-fullstack-challenge",
  "version": "1.0.0",
  "description": "Setup:     run `npm install` to install all the dependencies     run `npm start` to start the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "start": "node server/challenge-server.mjs",
    "up": "webpack && node server/challenge-server.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "react-router-dom": "^6.27.0",
    "webpack": "^5.95.0"
  },
  "devDependencies": {
    "webpack-cli": "^5.1.4"
  }
}
